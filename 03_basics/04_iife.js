/* IIFE :- Immediately Invoked Function Expression */
// ( Function definition) (Execution control)

// For the purpose of Database Connection
// don't need mess of global variable


(function chai () {
    console.log(`DB connected`);
}) ();  // to stop the execution context of iif we explicitly add ;

// () -> Function definition
// 2nd () -> Execution control

(function chaiaurcode () {
    console.log(`DB connected two`);
} )();


( (db_name) => {
    console.log(`${db_name} connected fine!`);
} )("my_database");


















































































































































































































